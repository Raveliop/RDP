version: "3.8"
services:
  bytebot:
    image: bytebotai/bytebot:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - bytebot_data:/data
volumes:
  bytebot_data:
